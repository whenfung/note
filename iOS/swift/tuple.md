---
title: tuple
---

定义变量的一个组合。

```swift
import UIKit

// 定义
var vector = (1, 2, 3)
var (x, y, z) = (1, 2, 3)


// 用序号访问第一个元素
vector.0
x

// 前缀变形
var 课程名 = (day:3, unit:"天", lang:"Swift", ver:"5.0")

// 用前缀获取某个元素
课程名.day
```

